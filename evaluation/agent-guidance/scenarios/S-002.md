# S-002: Trace lifecycle

The repository contains an active Goal titled "Ship fixture lifecycle". The
human explicitly authorizes only trace updates for this scenario; do not make
source changes, create an issue, create a pull request, or merge anything.

Using Belay:

1. Create a Decision titled "Use deterministic fixture data" and accept it.
2. Create Work titled "Prepare deterministic fixture data" and complete it.
3. Link both entries to the Goal with `fulfills`.
4. Link the Work to the Decision with `implements`.
5. Show the resulting entries and report their display IDs, relations, and
   statuses.
6. Report every Belay help call and non-zero exit category.
7. Report repository Skill state separately from runtime Skill recognition.

