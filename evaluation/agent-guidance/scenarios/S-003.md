# S-003: Sync and conflict recovery

The repository contains an intentional two-sided sync conflict for a Note
titled "Fixture source of truth". The Markdown body is the intended source of
truth; the SQLite body is an accidental local edit.

Using Belay:

1. Detect the conflict without overwriting either side.
2. Inspect enough of both representations to confirm the stated intended
   source.
3. Resolve only this entry by explicitly preferring Markdown.
4. Run sync again and show the Note to confirm the recovered body.
5. Report every Belay help call and non-zero exit category.
6. Report repository Skill state separately from runtime Skill recognition.

Do not delete trace history or rebuild the repository.

