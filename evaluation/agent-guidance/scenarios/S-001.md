# S-001: Context and planning orientation

This is a planning-only task. Do not implement, create an issue, create a pull
request, or change version-control state.

Use Belay to orient yourself to the goal titled "Improve fixture reliability"
and its related plan and decision. Start with compiled context, use targeted
search and show only as needed, classify the plan tier, and run deterministic
Goal lint. Report:

- the relevant display IDs and relationship;
- the tier and whether implementation is currently authorized;
- Goal lint outcome;
- every Belay help call and non-zero exit category;
- repository Skill state versus runtime Skill recognition.

Do not broadly scan `.belay/entries/`.

